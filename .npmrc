# lucide-react-native@0.475.0 declares react <=18 as a peer dep while this app
# is on react 19 (works fine at runtime; bun ignored the conflict too).
# Needed so `npm install` / `npm ci` succeed locally and in CI.
legacy-peer-deps=true
